python-telegram-bot>=21.0,<22
Pillow>=10.0
python-dotenv>=1.0
fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.33
google-generativeai>=0.8
openai>=1.50
pymongo>=4.9
certifi>=2024.7.4
# Freesolo Flash CLI (only for training runs) — Python 3.11-3.12 only.
# Install in a separate venv when you're ready to train:
#   python3.12 -m venv .venv-flash && ./.venv-flash/bin/pip install freesolo-flash
# The trippet runtime doesn't need it — inference uses `openai` above.
