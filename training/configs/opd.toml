model = "Qwen/Qwen3.5-4B"
algorithm = "opd"                 # on-policy distillation from a managed teacher (default GLM 5.2)
thinking = false

[environment]
id = "thesix/phoebe-agent"

[train]
epochs = 1
max_examples = 332
# lora_rank inherited from the warm-start adapter (rank 32)
# teacher_model = "glm-5.2"       # managed: glm-5.2 (default) | deepseek-v4-pro | kimi-k2.6
# Vet the teacher on THIS task before distilling (TRAINING.md): OPD can't
# exceed a teacher that can't diagnose the blocker itself. Warm-start from the
# SFT adapter first so the JSON format is already learned:
# init_from_adapter = "<sft-run-id>"
