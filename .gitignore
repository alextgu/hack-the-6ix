# Secrets — ignore EVERY .env variant (.env, .env.local, .env.prod, .env.backup…)
# except the committed placeholder template.
.env
.env.*
!.env.example

# Other secret material (defensive — never commit key/credential files)
*.pem
*.key
*-keypair.json
*_keypair.json
treasury*.json
service-account*.json
secrets.*

node_modules/
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv-bot/
.venv-flash/
venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/
